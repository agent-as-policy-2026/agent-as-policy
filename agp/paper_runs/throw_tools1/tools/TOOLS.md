# Tools left by previous sessions (index generated from the file headers)

| file | category | purpose | usage | assumptions |
|---|---|---|---|---|
| `extract_program_frames.py` | process | Extract camera frames aligned to a buffered program and make labeled contact sheets. | `python3 knowledge/tools/extract_program_frames.py REPORT CAMERA OUTPUT_PREFIX --start SECONDS --end SECONDS [--stride N] (run from the session directory)` | Camera CSV frame indices match video frames; shared monotonic clock; requires ffmpeg and Pillow. |
| `make_profile.py` | process | Generate a sampled J4-only trapezoidal or triangular buffered program without commanding the robot. | `python3 knowledge/tools/make_profile.py OUTPUT --accel RAD_S2 --ramp SECONDS [--cruise SECONDS] [--hold SECONDS] [--open SECONDS --fraction FRACTION]    (run from the session directory)` | 0.02 s sampling; durations must be nonnegative multiples of 0.02 s with positive ramp; default hold 0.4 s; default event fraction 1; caller must preview for limits and clearance. |
| `seek_program_frames.py` | process | Extract time-aligned recording frames using indexed video seeking and write contact sheets. | `python3 knowledge/tools/seek_program_frames.py REPORT CAMERA PREFIX START END    (run from the session directory)` | OpenCV and Pillow installed; CSV frame indices match video indices; shared monotonic clocks; seeking must return the requested index. |
| `summarize_program.py` | process | Summarize measured joint and jaw response from a buffered execution report. | `python3 knowledge/tools/summarize_program.py REPORT [--step SECONDS] (run from the session directory)` | Seven-element command, feedback and velocity arrays; arm units radians and rad/s, jaw fraction; trace clocks follow README_interface.md. |
