# Tools left by previous sessions (index generated from the file headers)

| file | category | purpose | usage | assumptions |
|---|---|---|---|---|
| `blobplane.py` | geometry | take the colour blob containing a seed pixel in a wrist or top capture, project its outline onto a horizontal plane z, and report centroid, area, min-area rectangle and circle fits of the outer contour and the largest hole | `python3 knowledge/tools/blobplane.py N wrist|top u v z "hmin,hmax,smin,smax,vmin,vmax"    (run from the session directory; needs numpy, opencv, pillow)` | OpenCV HSV ranges (H 0-179); restrict V/S so only the face at height z is selected (side walls seen obliquely bias the outline); uses geo.py from the same directory |
| `facefit.py` | geometry | locate an object's flat top face in a wrist capture (colour mask AND depth within a band of the seed's height) and report its centre, height, outline radius, polygon edge angle and inner-hole circle | `python3 knowledge/tools/facefit.py N u v "hmin,hmax,smin,smax,vmin,vmax" [band_m=0.003]    (run from the session directory; needs numpy, opencv, pillow)` | the top face has stereo texture (valid depth); OpenCV HSV ranges (H 0-179); edge angle is measured from base +x; outline points are deprojected to the plane z_top |
| `geo.py` | geometry | camera geometry helpers for frames/NNNN_calib.json: pixel->plane, wrist pixel->3D via saved depth, base point->pixel | `python3 knowledge/tools/geo.py N wrist|top u v [plane_z]   |   python3 knowledge/tools/geo.py N wrist|top pix x y z    (run from the session directory; needs numpy)` | current working directory is the session directory; wrist pixels treated as undistorted pinhole (distortion ignored, ~1px effect); depth uses a 5x5 median of valid pixels |
| `hexfit.py` | geometry | fit a regular hexagon (nut, hex head, hex prism) to a colour blob in a wrist or top capture projected onto plane z; report centre, radii, vertex direction and the flat-normal directions a parallel gripper should close along | `python3 knowledge/tools/hexfit.py N wrist|top u v z "hmin,hmax,smin,smax,vmin,vmax"    (run from the session directory; needs numpy, opencv, pillow)` | OpenCV HSV ranges (H 0-179); blob = whole hex (side walls seen obliquely inflate radii by a few mm and may bias the centre; view from nearly above); orientation from the 6-fold angular moment of the outline; for a tool-down grasp with yaw psi from (w0,x1,y0,z0) the fingers close along base angle 90+psi deg, so psi = flat_normal - 90 (mod 60); uses geo.py from knowledge/tools |
| `inhand.py` | geometry | measure where a held object sits relative to the grasp point (TOOL frame) from one wrist capture taken after lifting it, so placement can correct for in-hand offset | `python3 knowledge/tools/inhand.py N "hmin,hmax,smin,smax,vmin,vmax" [v_min_px=200]    (run from the session directory; needs numpy, opencv, pillow)` | the held object appears in the lower image rows (v >= v_min_px) between the fingers; top band = points within 3 mm of the highest; circle fit suits round rims/rings, for polygons read the extents instead |
| `inhand_face.py` | geometry | locate a held (or about-to-be-grasped) object's flat TOP face in the TOOL frame from one wrist capture, using colour AND a depth band around the topmost level so side walls are excluded; gives the in-hand offset needed for precise placement | `python3 knowledge/tools/inhand_face.py N "hmin,hmax,smin,smax,vmin,vmax" [band_m=0.002] [v_min_px=150]    (run from the session directory; needs numpy, opencv, pillow)` | top face has stereo texture (valid depth); largest connected face region is the object (restrict v_min_px / HSV so other same-colour objects are excluded); the lower image border may cut the face - then use the far edge/vertex and the known object size instead of the midpoint; uses geo.py from knowledge/tools |
| `rimfit.py` | geometry | fit a circular top rim (cylinder/shaft top) at a KNOWN height from the colour outline in a wrist capture, for texture-less tops where depth is missing | `python3 knowledge/tools/rimfit.py N u v z_top "hmin,hmax,smin,smax,vmin,vmax" [r_expected_m]    (run from the session directory; needs numpy, opencv, pillow)` | HSV range should select ONLY the top face (e.g. use a high V floor so the darker side wall is excluded); z_top from an earlier depth measurement; RANSAC inlier tolerance 0.6 mm; optional radius constraint +-1.5 mm |
| `viewpose.py` | geometry | compute a tool-down grasp-point pose (any yaw) that makes the wrist camera look at a given base-frame point from a given distance | `python3 knowledge/tools/viewpose.py x y z [dist_m=0.15] [yaw_deg=0]    (run from the session directory; needs numpy)` | wrist camera sits at (0.0728, 0.0021, -0.0613) m in the tool frame with optical axis (-0.428, -0.021, 0.903) in the tool frame (measured from calib pose vs ee_pose on this robot; re-check against frames/NNNN_calib.json if the camera mount changed); yaw is about base z from the canonical down rotation (w0,x1,y0,z0) |
