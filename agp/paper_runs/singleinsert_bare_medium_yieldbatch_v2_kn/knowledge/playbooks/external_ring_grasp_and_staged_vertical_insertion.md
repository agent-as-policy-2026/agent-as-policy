# playbook: external ring grasp and staged vertical insertion

<!-- from session consolidation_20260907_211612, merged 2026-09-07 -->
Use the complete reported height sequence from inputs/20260907_205012_paper_singleinsert_bare_medium_yieldbatch_v2_01 (server.log commands #1–#24 all ok=True). **P** denotes scene-dependent XY; **H/W/O** denotes reusable height/width/orientation evidence for the same parts and table, subject to remeasurement. Commands below are a parameterized record, not an executable motion script: exact rotation, approach/lift heights and motion mode were not preserved.

1. Observe with `python3 robot_client.py . state` and `python3 robot_client.py . frames`; localize the smooth blue ring and upright white post using wrist depth/calibration, consulting top imagery for identity. Default `frames` calls capture both cameras and depth per the interface; historical camera arguments are unknown. Estimate table and part heights before selecting target planes.
2. Set **P** ring center `(RX,RY)` and post center `(PX,PY)` from current observations; historical values were `(0.173,-0.069)` and `(0.294,-0.160)` m. Use geometry.py for positive-depth point samples and top-ray projection at a measured height; use measure.py for ROI bounds. Do not use an object's base-plane height to project its elevated top.
3. Approach above `(RX,RY)` with an externally open, **O** downward-facing gripper. Approach height, initial opening command and exact quaternion are unavailable; derive these from the current scene/state. The logged sequence contains approach moves before closing, but no initial gripper-open call.
4. At the ring, use `python3 robot_client.py . move_ee '<pose JSON>'`, with `position={x:RX,y:RY,z:-0.020}` and the validated downward rotation. **H** grasp z=-0.020 m was successful. Then `python3 robot_client.py . gripper '{"action":"close"}'`; **W** observed contact opening was 0.0519 m, not a commanded width target.
5. Lift clear of the table/post using `move_ee`, inspect with `frames`, then translate above `(PX,PY)`. Lift height was not recorded; choose clearance from measured geometry and the held ring rather than inventing a historical value.
6. Use successive `move_ee` pose JSONs with `position={x:PX,y:PY,z:Z}` and the maintained downward rotation for **H** `Z=0.082, 0.050, 0.015, -0.012` m. The log places `frames` after the first three levels; the report describes white entering the ring during descent. No nonzero lateral correction is documented. Stop to reassess if actual engagement differs from these observations.
7. Release with `python3 robot_client.py . gripper '{"action":"open"}'` at **H** z=-0.012 m; **W** reported open width was 0.0948 m. Use `move_ee` to raise to **H** z=0.150 m and `frames` to inspect the released pair.
8. Withdraw with `move_ee` to a scene-clear **P** location (historically ≈ `(0.220,0.035,0.200)` m), then `frames` and `status`. Confirm the ring remains around the white base, upright and unsupported, with the gripper open and arm clear. Keep the unrelated pair and other arm clear throughout.
